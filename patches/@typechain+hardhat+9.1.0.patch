diff --git a/node_modules/@typechain/hardhat/dist/index.js b/node_modules/@typechain/hardhat/dist/index.js
index 9f14a06..a8dabf8 100644
--- a/node_modules/@typechain/hardhat/dist/index.js
+++ b/node_modules/@typechain/hardhat/dist/index.js
@@ -29,6 +29,7 @@ const config_1 = require("hardhat/config");
 const contract_names_1 = require("hardhat/utils/contract-names");
 const config_2 = require("./config");
 const constants_1 = require("./constants");
+const path_1 = require("path");
 const taskArgsStore = { noTypechain: false, fullRebuild: false };
 (0, config_1.extendConfig)((config) => {
     config.typechain = (0, config_2.getDefaultTypechainConfig)(config);
@@ -72,7 +73,9 @@ const taskArgsStore = { noTypechain: false, fullRebuild: false };
     }
     const cwd = config.paths.root;
     const { glob } = await Promise.resolve().then(() => __importStar(require('typechain')));
-    const allFiles = glob(cwd, [`${config.paths.artifacts}/!(build-info)/**/+([a-zA-Z0-9_]).json`]);
+    const artifactPattern = `${config.paths.artifacts}/**/+([a-zA-Z0-9_]).json`;
+    const buildInfoNeedle = `${path_1.sep}build-info${path_1.sep}`;
+    const allFiles = glob(cwd, [artifactPattern]).filter((artifactPath) => !artifactPath.includes(buildInfoNeedle));
     if (typechainCfg.externalArtifacts) {
         allFiles.push(...glob(cwd, typechainCfg.externalArtifacts, false));
     }
